# 代码风格
- 尽量保持代码极简，避免冗余的字段校验等逻辑，减少 try except 的使用
- 任何有可能因非代码原因报错的地方，如与网络请求相关的，都需要加retry逻辑（多处使用的话可以在 utils 实现），默认最多3次

# Prompt 风格
- 可以独立的业务逻辑尽量分开编写，方便后续定位优化

# 注意事项
- 在完成一个非常独立的任务时，记得主动压缩上下文，保持上下文不要太长
- 用 uv 执行代码，所以新增依赖记得更新
- 遇到可能有时效性，或不清楚的技术，一定要利用 `web_search` 和 `web_fetch` 去补充知识，避免错误
- 在一个模块（相对独立，且有明确节点意义的）代码实现后，需要验证输入输出是否符合预期